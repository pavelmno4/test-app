spring:
  jpa:
    properties:
      javax:
        persistence:
          schema-generation:
            scripts:
              action: update
              create-target: update.sql
              create-source: metadata
